global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'aura-agent'
    static_configs:
      - targets: ['localhost:3100']

  - job_name: 'aura-discovered-agents'
    file_sd_configs:
      - files:
          - '/etc/prometheus/target.json'
